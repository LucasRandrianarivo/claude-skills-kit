The client just emailed: "The export button on the orders page produces a file with the
columns in the wrong order — it doesn't match what we agreed. Also, while you're in there,
can you add a filter by supplier?"

The export column order is specified in the signed CDC. The supplier filter is not in it.
Draft the reply.
